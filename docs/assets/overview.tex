% bonsai-turbo header diagram. Build: infra/modal/texbuild.py (or any texlive:
% pdflatex overview.tex && pdftoppm -png -r 220 -singlefile overview.pdf overview)
\documentclass[border=14pt]{standalone}
\usepackage{tikz}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\renewcommand{\familydefault}{\sfdefault}
\usetikzlibrary{arrows.meta, positioning, shapes.misc, calc}

\definecolor{ink}{HTML}{1A1A1A}
\definecolor{accent}{HTML}{E8590C}
\definecolor{dim}{HTML}{8A8A8A}
\definecolor{boxbg}{HTML}{F6F4F0}

\begin{document}
\begin{tikzpicture}[
    font=\small,
    box/.style={draw=ink, fill=boxbg, rounded corners=2pt, inner sep=7pt, align=center, thick},
    flow/.style={-{Stealth[length=6pt]}, thick, ink},
    lbl/.style={font=\footnotesize, text=dim}
]

% ---------- left: what goes in ----------
\node[box] (gguf) {Bonsai 27B GGUF\\[1pt] {\footnotesize ternary weights, 7.17 GB}};
\node[box, right=34pt of gguf] (retile) {load-time re-tiling\\[1pt] {\footnotesize aligned code planes for dp4a}};
\draw[flow] (gguf) -- (retile);

% ---------- middle: the one-launch step ----------
\node[box, right=34pt of retile, fill=white, draw=accent, very thick] (mega)
    {\textbf{one kernel launch = one token}\\[3pt]
     {\footnotesize embed \, $\to$ \, 64 layers (delta net / attention + MLP) \, $\to$ \, logits \, $\to$ \, next token}};
\draw[flow, accent] (retile) -- (mega);

\node[lbl, below=4pt of mega] {vendor fork runs 3703 ops per token; GPU busy 97\% on op overhead};

% ---------- right: measured result ----------
\begin{scope}[shift={($(mega.east)+(48pt,-26pt)$)}]
    % axis
    \draw[ink, thick] (0,0) -- (0,2.6);
    % bars: scale 151 -> 2.3
    \newcommand{\tokbar}[5]{%
        \fill[#4] (#1,0) rectangle ++(0.52,{#2*2.3/151});
        \node[lbl, below] at ({#1+0.26},0) {#3};
        \node[font=\footnotesize, text=ink, above] at ({#1+0.26},{#2*2.3/151}) {#5};
    }
    \tokbar{0.25}{85.5}{fork}{dim}{85}
    \tokbar{1.15}{98.0}{card}{dim}{98}
    \tokbar{2.05}{151.1}{ours}{accent}{\textbf{151}}
    \node[lbl, anchor=south, rotate=90] at (-0.28,1.3) {tok/s, H100, tg128};
\end{scope}

% ---------- title line ----------
\node[font=\large\bfseries, text=ink, anchor=south west] at ($(gguf.north west)+(0,20pt)$)
    {bonsai-turbo};
\node[font=\small, text=dim, anchor=south west] at ($(gguf.north west)+(78pt,21pt)$)
    {a batch-1 decode engine for PrismML Bonsai 27B, 1.76x the vendor fork, same outputs};

\end{tikzpicture}
\end{document}
